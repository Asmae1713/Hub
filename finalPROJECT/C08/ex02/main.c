#include "ft_abs.h"
#include <stdio.h>

int main() {
	    int num1 = -10;
	        int num2 = 5;

		    printf("Absolute value of %d is %d\n", num1, ABS(num1));
		        printf("Absolute value of %d is %d\n", num2, ABS(num2));
			    return 0;
}

